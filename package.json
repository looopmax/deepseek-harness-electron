{
  "name": "deepseek-dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the DeepSeek Harness desktop app. bun manages the app workspace (apps/*); the deepseek-harness git submodule under packages/ is an upstream pnpm workspace that installs its own dependencies.",
  "workspaces": [
    "apps/*"
  ],
  "overrides": {
    "@deepseek-ai/dsh-tool-session-query": "npm:debug@4"
  },
  "scripts": {
    "start": "bun run --cwd apps/client start",
    "dev": "bun run --cwd apps/client start",
    "pack": "bun run --cwd apps/client pack",
    "dist": "bun run --cwd apps/client dist",
    "harness:install": "pnpm -C packages/deepseek-harness install",
    "harness:build": "pnpm -C packages/deepseek-harness run build"
  }
}
